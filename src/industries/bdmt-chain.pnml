switch (FEAT_INDUSTRIES, SELF, location_coal_mine_distance_coal_mine, industry_distance(COAL_MINE_INDEX)) {
        0..36: return string(STR_ERR_LOCATION_COAL_MINE);
        CB_RESULT_LOCATION_ALLOW;
}

switch (FEAT_INDUSTRIES, SELF, location_coal_mine_distance_brickworks, industry_distance(BRICKWORKS_INDEX)) {
        0..36: return string(STR_ERR_LOCATION_BRICKWORKS);
        location_coal_mine_distance_coal_mine;
}

item(FEAT_INDUSTRIES, industry_coal_mine, COAL_MINE_INDEX) {
    property {
        substitute: 0;
        override: 0;
        cargo_types: [
            produce_cargo("COAL", 12)
        ];
        prob_map_gen: 8;
        prob_in_game: 3;
        life_type: IND_LIFE_TYPE_EXTRACTIVE;
        map_colour: ARRAY_EVAL(INDUSTRY_COLOR_INDEX, COAL_MINE_INDEX);
        nearby_station_name: string(STR_INDUSTRY_COAL_MINE_STATION_NAME);
    }
    graphics {
       location_check: location_coal_mine_distance_brickworks;
    }
}

switch (FEAT_INDUSTRIES, SELF, location_peatlands_distance_peatlands, industry_distance(PEATLANDS_INDEX)) {
        0..36: return string(STR_ERR_LOCATION_PEATLANDS);
        CB_RESULT_LOCATION_ALLOW;
}

switch (FEAT_INDUSTRIES, SELF, location_peatlands_distance_brickworks, industry_distance(BRICKWORKS_INDEX)) {
        0..36: return string(STR_ERR_LOCATION_BRICKWORKS);
        location_peatlands_distance_peatlands;
}

item(FEAT_INDUSTRIES, industry_peatlands, PEATLANDS_INDEX) {
    property {
        substitute: 17;
        name: string(STR_INDUSTRY_PEATLANDS_NAME);
        cargo_types: [
            produce_cargo("PEAT", 15)
        ];
        prob_map_gen: 8;
        prob_in_game: 3;
        life_type: IND_LIFE_TYPE_EXTRACTIVE;
        map_colour: ARRAY_EVAL(INDUSTRY_COLOR_INDEX, PEATLANDS_INDEX);
        nearby_station_name: string(STR_INDUSTRY_PEATLANDS_STATION_NAME);
    }
    graphics {
       location_check: location_peatlands_distance_brickworks;
    }
}

item(FEAT_INDUSTRYTILES, brickworks_tile_1) {
    property {
        substitute:      121;
        override:        121;
        accepted_cargos: [[PASS, 1]];
        special_flags: bitmask(INDTILE_FLAG_ACCEPT_ALL);
    }
}

item(FEAT_INDUSTRYTILES, brickworks_tile_2) {
    property {
        substitute:      122;
        override:        122;
        accepted_cargos: [[PASS, 1]];
        special_flags: bitmask(INDTILE_FLAG_ACCEPT_ALL);
    }
}

item(FEAT_INDUSTRYTILES, brickworks_tile_3) {
    property {
        substitute:      123;
        override:        123;
        accepted_cargos: [[PASS, 1]];
        special_flags: bitmask(INDTILE_FLAG_ACCEPT_ALL);
    }
}

item(FEAT_INDUSTRYTILES, brickworks_tile_4) {
    property {
        substitute:      124;
        override:        124;
        accepted_cargos: [[PASS, 1]];
        special_flags: bitmask(INDTILE_FLAG_ACCEPT_ALL);
    }
}

switch (FEAT_INDUSTRIES, SELF, location_brickworks_distance_coal_mine, industry_distance(COAL_MINE_INDEX)) {
        0..36: return string(STR_ERR_LOCATION_COAL_MINE);
        CB_RESULT_LOCATION_ALLOW;
}

switch (FEAT_INDUSTRIES, SELF, location_brickworks_distance_peatlands, industry_distance(PEATLANDS_INDEX)) {
        0..36: return string(STR_ERR_LOCATION_PEATLANDS);
        location_brickworks_distance_coal_mine;
}

switch (FEAT_INDUSTRIES, SELF, location_brickworks_distance_brickworks, industry_distance(BRICKWORKS_INDEX)) {
        0..36: return string(STR_ERR_LOCATION_BRICKWORKS);
        location_brickworks_distance_peatlands;
}

switch (FEAT_INDUSTRIES, SELF, location_brickworks_distance_shop, industry_distance(SHOP_INDEX)) {
        0..36: return string(STR_ERR_LOCATION_SHOP);
        location_brickworks_distance_brickworks;
}

item(FEAT_INDUSTRIES, industry_brickworks, BRICKWORKS_INDEX) {
    property {
        substitute: 23;
        name: string(STR_INDUSTRY_BRICKWORKS_NAME);
        cargo_types: [
            accept_cargo("COAL", produce_cargo("BDMT", 1)),
            accept_cargo("PEAT", produce_cargo("BDMT", 1))
        ];
        prob_map_gen: 5;
        prob_in_game: 2;
        life_type: IND_LIFE_TYPE_PROCESSING;
        nearby_station_name: string(STR_INDUSTRY_BRICKWORKS_STATION_NAME);
    }
    graphics {
       location_check: location_brickworks_distance_shop;
    }
}
