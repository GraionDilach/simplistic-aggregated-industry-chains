switch (FEAT_INDUSTRIES, SELF, location_fizzy_drink_chain_distance_cola_wells, industry_distance(COLA_WELLS_INDEX)) {
        0..24: return string(STR_ERR_LOCATION_COLA_WELLS);
        CB_RESULT_LOCATION_ALLOW;
}

switch (FEAT_INDUSTRIES, SELF, location_fizzy_drink_chain_distance_bubble_generator, industry_distance(BUBBLE_GENERATOR_INDEX)) {
        0..24: return string(STR_ERR_LOCATION_BUBBLE_GENERATOR);
        location_fizzy_drink_chain_distance_cola_wells;
}

switch (FEAT_INDUSTRIES, SELF, location_fizzy_drink_chain_distance_fizzy_drink_factory, industry_distance(FIZZY_DRINK_FACTORY_INDEX)) {
        0..24: return string(STR_ERR_LOCATION_FIZZY_DRINK_FACTORY);
        location_fizzy_drink_chain_distance_bubble_generator;
}

switch (FEAT_INDUSTRIES, SELF, location_fizzy_drink_chain_distance_shop, industry_distance(SHOP_INDEX)) {
        0..24: return string(STR_ERR_LOCATION_SHOP);
        location_fizzy_drink_chain_distance_fizzy_drink_factory;
}

if (enable_converted_toyland)
{
    item(FEAT_INDUSTRIES, industry_cola_wells, COLA_WELLS_INDEX) {
        property {
            substitute: 29;
            override: 29;
            cargo_types: [
                produce_cargo("COLA", 12),
            ];
            prob_map_gen: 5;
            prob_in_game: 3;
            life_type: IND_LIFE_TYPE_EXTRACTIVE;
            nearby_station_name: string(STR_INDUSTRY_COLA_WELLS_STATION_NAME);
        }
        graphics {
           location_check: location_fizzy_drink_chain_distance_shop;
        }
    }
}

if (enable_converted_toyland)
{
    item(FEAT_INDUSTRIES, industry_bubble_generator, BUBBLE_GENERATOR_INDEX) {
        property {
            substitute: 34;
            override: 34;
            cargo_types: [
                produce_cargo("BUBL", 13),
            ];
            prob_map_gen: 5;
            prob_in_game: 3;
            life_type: IND_LIFE_TYPE_EXTRACTIVE;
            nearby_station_name: string(STR_INDUSTRY_BUBBLE_GENERATOR_STATION_NAME);
        }
        graphics {
           location_check: location_fizzy_drink_chain_distance_shop;
        }
    }
}

item(FEAT_INDUSTRYTILES, fizzy_drink_factory_tile_1)
{
    property
    {
        substitute: 156;
        override: 156;
        accepted_cargos: [];
        special_flags: bitmask(INDTILE_FLAG_ACCEPT_ALL);
    }
}

item(FEAT_INDUSTRYTILES, fizzy_drink_factory_tile_2)
{
    property
    {
        substitute: 157;
        override: 157;
        accepted_cargos: [];
        special_flags: bitmask(INDTILE_FLAG_ACCEPT_ALL);
    }
}

item(FEAT_INDUSTRYTILES, fizzy_drink_factory_tile_3)
{
    property
    {
        substitute: 158;
        override: 158;
        accepted_cargos: [];
        special_flags: bitmask(INDTILE_FLAG_ACCEPT_ALL);
    }
}

item(FEAT_INDUSTRYTILES, fizzy_drink_factory_tile_4)
{
    property
    {
        substitute: 159;
        override: 159;
        accepted_cargos: [];
        special_flags: bitmask(INDTILE_FLAG_ACCEPT_ALL);
    }
}

if (enable_converted_toyland)
{
    item(FEAT_INDUSTRIES, industry_fizzy_drink_factory, FIZZY_DRINK_FACTORY_INDEX) {
        property {
            substitute: 33;
            override: 33;
            cargo_types: [
                accept_cargo("COLA", produce_cargo("FZDR", 1)),
                accept_cargo("BUBL", produce_cargo("FZDR", 1))
            ];
            prob_map_gen: 4;
            prob_in_game: 3;
            life_type: IND_LIFE_TYPE_PROCESSING;
            nearby_station_name: string(STR_INDUSTRY_FIZZY_DRINK_FACTORY_STATION_NAME);
        }
        graphics {
           location_check: location_fizzy_drink_chain_distance_shop;
        }
    }
}
